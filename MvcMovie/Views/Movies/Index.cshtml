@model MvcMovie.Models.MovieSearchViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Movie Database</h1>
<!-- Search Form *Provides the functionality to search any of the fields available-->
<form asp-controller="Movies" asp-action="Index" method="get">
    <p>
        
        <!-- search by title-->
        <input type="submit" value="Search Movie Name" />
        <input type="text" asp-for="SearchString" />

        <!-- search by release date-->
        <select asp-for="MovieReleaseDate" asp-items="Model.ReleaseDates">
            <option value="">Date</option>
        </select>
        
        <!-- search by genre-->
        <select asp-for="MovieGenre" asp-items="Model.Genres">
            <option value="">Genre</option>
        </select>
        
        <!-- search by price-->
        <select asp-for="MoviePrice" asp-items="Model.Prices">
            <option value="">Price</option>
        </select>

        <!-- search by rating-->
        <select asp-for="MovieRating" asp-items="Model.Ratings">
            <option value="">Rating</option>
        </select>
    </p>
</form>
<!-- Add Movie button -->
<p>
    <a asp-action="Create">Add Movie</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Price)
            </th>
            <!-- Added Rating to view template to display, create, and edit Rating in browser-->
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Rating)
            </th>
            <th>Usage</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Movies!) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <!-- Added Rating to view template to display, create, and edit Rating in browser-->
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
